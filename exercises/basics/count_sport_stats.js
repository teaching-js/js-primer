/*
   Given a list of sports data count the number of
   matches and tries each player in the list has and return
   an object containing the sum
*/
function count_stats(list) {
   /* HINT use reduce */
}
